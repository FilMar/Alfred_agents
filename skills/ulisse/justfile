# Ulisse — skill router recipes.
# This skill wraps its own script. SKILL.md calls only this justfile.

set fallback := true

SMITH_DIR := justfile_directory()

default:
    @just --list

# Print every skill's name and full description, read live from the skills folder.
list:
    @python3 "{{SMITH_DIR}}/scripts/list_skills.py" "{{SMITH_DIR}}/.."
