# Log

```yaml
tags: [log, history]
sources: []
updated: 2026-08-26
```

## Log

## [2026-08-26] revision | Roadmap aligned to the area-grouped template

[roadmap](roadmap) rewritten to the new template: tasks grouped under an H3 per area (memory, th, rasp, cockpit, skills), one line per task, ten words or fewer, no code names, a `[detail](page)` link per line, one plain status sentence for paused areas. Phase labels dropped. Content changes: the stale "Orchestrator Phase 2" line (Phase 2 is done since 2026-07-17) became "finish the orchestrator" pointing at Phases 3-4; two planned items already recorded elsewhere joined the list — the search relevance cutoff ([ti_module](ti_module)) and the skill router pass ([skill_migration](skill_migration)). The career coach task has no detail page; flagged in the line. Inbound links that cited "Phase 7"/"Phase 8" repointed to areas in procedural_memory_gaps, tb_on_rasp, stateless_cockpit, rasp_node.

## [2026-08-25] revision | Language aligned to the easy-English glossary

Re-read the whole wiki and aligned the language to the easy-English style in the glossary (short sentences, active voice, common words). The main work was [log](log): translated the historical entries still written in Italian (2026-07-15 to 2026-07-17, plus 2026-07-02, 2026-06-14, 2026-06-06) into easy English, and fixed the Italian frontmatter tag `storico` → `history`. All other pages were already in English and needed no change. One literal tool output stays as-is: `th run`'s warning string in [th_cli](th_cli) is a verbatim stderr line from `tools/th/src/runner.ts` (`warn: bwrap non disponibile — esecuzione SENZA sandbox`) — translating it in the wiki would misrepresent the tool.

## [2026-08-18] revision | Frontmatter aligned to fenced yaml

All 18 pages migrated to the new convention: fenced `yaml` block at top, no `## Frontmatter` heading, no `---` delimiters. Converted: agenti, architettura, index, log, roadmap, th_cli (bare yaml under heading); procedural_memory_gaps, rasp_node, style_dual_entrypoint, style_tb_ti_layering, tb_on_rasp (heading removed); orchestrator_overview, roadmap_orchestrator, skill_migration, skill_pattern (`---` style). `updated` bumped on all touched pages.

## [2026-08-18] revision | Roadmap flattened to a task list

[roadmap](roadmap) rewritten from a phase-by-phase document to a flat task list — one line per task, deep dives in linked files. Phase numbers kept as labels (referenced across the wiki). Cross-references updated in architettura, procedural_memory_gaps, rasp_node, stateless_cockpit, tb_on_rasp; index gained the missing roadmap entry.

## [2026-08-18] ingest | Router pass planned — recorded in skill_migration

Late addition, same day: annibale's flows/ folded into references/ + scripts/; atlante's chart generators moved into scripts/ (lint learned `// desc:` and library files). Next structural pass designed and recorded in [skill_migration](skill_migration) (router SKILL.md ~50-60 lines, three design rules) — deliberately deferred until the direct-CLI convention has seen real use.

## [2026-08-18] revision | Fleet migration complete — 18 skills off justfiles

All 18 skills in [skill_migration](skill_migration) migrated and committed, one commit per skill. Fleet-wide lint clean (21/21 OK), zero justfiles and zero guard.sh left under skills/. Clio's systemd service (installed and in-repo template) repointed to scripts/backup_all.sh; timer stays disabled. Still open, deliberately deferred: alfred.md, CLAUDE.md quick-lookups, dotfiles pi-just removal, ti rule rewrites.

## [2026-08-18] ingest | Skill convention: justfile layer retired

- Rewrote [skill_pattern](skill_pattern): SKILL.md calls the CLI directly; deterministic multi-step sequences live in `scripts/` (executable, `# desc:` header); deep dives in `references/`. Efesto owns and lints the convention.
- New page [skill_migration](skill_migration): ordered tasklist of the 18 skills still on justfiles (christopher and mose first — CLAUDE.md quick-lookups), plus post-fleet cleanup (alfred.md, CLAUDE.md, dotfiles, ti rules).
- [agenti](agenti): roster command updated to `python3 skills/efesto/scripts/roster.py`.
- [index](index): skill_pattern row updated, skill_migration row added.

## [2026-08-10] revision | Stateless Cockpit pivots to a pi extension — web UI dropped

Supersedes the 2026-08-09 pause, recorded in [stateless_cockpit](stateless_cockpit)'s new Pivot section. Decision: drop the Hono/HTMX web UI and the external app spawning a fresh pi SDK session per turn; cockpit becomes a TypeScript extension of pi itself, run in RPC mode (stdin/stdout JSONL) as one persistent process. This eliminates the spawn-per-turn cost diagnosed in the pause by construction — the process never dies between turns, so there is nothing to keep warm. Verified against pi's own `extensions.md`/`compaction.md` docs that the whole bank/ledger/condense design maps onto native hooks: `session_before_compact` (custom summary/ledger logic, replaces bespoke condense), `before_agent_start`/`context` (tb/ti retrieval injection per turn), `pi.registerCommand()` (`/mem`, `/safe`, `/clean`, hats, `/edit-memory`), `pi.registerTool()` (`emit_widget`, if still needed without a browser to render it in). Explicit accepted trade-off: the original "web UI from any device on the tailnet" requirement is dropped — access becomes RPC/CLI, a phone needs an SSH/terminal client, not a browser tab. `feature/cockpit-skeleton` untouched, no merge/cleanup obligation. No implementation started — decision only.

## [2026-08-09] revision | Stateless Cockpit paused after first end-to-end test

Not abandoned — paused pending a decision, recorded in [stateless_cockpit](stateless_cockpit)'s new Pause section. The build turned out to be further along than the wiki showed: real implementation on branch `feature/cockpit-skeleton` (never merged), through commit `08c6f73` — Hono server + HTMX UI + command handlers, `bank`/`router`/`turn` modules with tests, working `retrieve`/`condense`/`runAgent` edges. First end-to-end test (cloud model, not the local target) came back slow and clunky. Diagnosis from conversation: slowness is mechanical — `runAgent` spawns a fresh `bun` process every turn (fork + bwrap + SDK boot + session init) before the model even answers; the clunkiness isn't explained by model size in this test (cloud 300B-class, not the 16GB-VRAM local target) and stays open pending a retest on the real target model. Not reconsidered: the target deployment is local within 16GB VRAM (candidate `dwarfstar`), where the long-history degradation problem the bank/ledger/condense design solves is still real. Agreed next steps before resuming: (1) model-independent fix — persistent warm process instead of spawn-per-turn, rebuilding only the per-turn context from the bank, not the OS process or a growing SDK session; (2) re-run the ~20-30 turn quality test against the actual local candidate model, not cloud, to settle whether the memory machinery is needed at that scale; (3) decide resume-as-is vs. simplify only after that test. Branch left untouched, no merge/cleanup obligation. Updated: [roadmap](roadmap) (worksite 4 marked paused).

## [2026-08-08] revision | Stateless Cockpit founded — Matrix layer superseded

Foundation session via `piano`; `tools/cockpit/` created (README.md, justfile, CLAUDE.md). All open questions of [stateless_cockpit](stateless_cockpit) closed, recorded in its new Foundation decisions section: Pillar 5 risk accepted (security deferred, tailnet-only, account step later; `th` sandbox covers filesystem not network); **Matrix shuts down entirely** — the cockpit replaces the Matrix chat and pi-web, notifications become a feed at next opening, `!tb search` becomes a turn; agent via pi SDK (fresh session per turn, full toolset — direct Ollama calls discarded as toolless; injection→shell risk reopened and accepted); `/mem` markdown banks (delta summary, append-only ledger with user-confirmed entries, 3-exchange verbatim tail, per-bank `/safe` profile, default write `essays/dot_file/alfred`; no SQLite); v1 entirely on the Rasp, `/wake` + Ollama URL move as future config-only step; hats from `tools/th/hats/`, annibale out of v1; `/bash` is a user terminal, audit question dropped; sshfs rejected. Updated: [roadmap](roadmap) (worksite 4 founded, Matrix shared-component superseded), [orchestrator_overview](orchestrator_overview) (Interactive pi Chat marked superseded), [index](index).

## [2026-08-08] ingest | Stateless Cockpit — proposed next project

New page [stateless_cockpit](stateless_cockpit) from a session discussion. Tailnet-only web UI with episodic stateless execution: each turn rebuilds context (rolling hot memory + tb/ti retrieval), fresh agent call, async re-condensation. Includes the review amendments (raw tail + summary, append-only facts ledger, merge-not-rewrite, no LLM-generated HTML — JSON widget schemas instead, reuse of existing harness/atlante) and open questions, chief among them the conflict with Pillar 5 of [orchestrator_overview](orchestrator_overview) (agent chat is Matrix-only by design; a tailnet web UI reopens the single-auth-channel hole). Roadmap worksite 4 added; not started, foundation via `piano`.

## [2026-07-28] ingest | Proposal: search score-cutoff (ti/tb) + UserPromptSubmit hook for structural rule retrieval

Design discussion, not yet implemented, documented in [ti_module](ti_module). Two items: (1) add a `--min-score` option natively to `search` in `tools/ti/src/` and `tools/tb/src/` (currently only `--limit`/`--tags` exist, no relevance threshold) — filtering belongs in the tool source, not as a `jq` post-filter in the wrapping `mose`/`christopher` justfiles; (2) a Claude Code `UserPromptSubmit` hook that runs `ti search`/`tb search` (with the cutoff above) on every incoming prompt and injects the results as context, replacing pull-based retrieval (agent must remember to query `ti`) with structural injection. Motivated by two new `ti` rules added this session on justfile discipline (never bypass a skill's justfile for its underlying CLI; propose a new recipe rather than an ad-hoc command when one is missing) — the concrete case that exposed the "rule is lost if nobody queries `ti`" gap already flagged in `ti_module`'s governance section. Open questions: relevance-threshold tuning, added per-prompt latency.

## [2026-07-27] ingest | tb/ti coding standards migrated from tools/*/CLAUDE.md

`tools/tb/CLAUDE.md` (layered architecture, constants placement, dead code, DRY, file size, readability) had no wiki equivalent — migrated verbatim into [style_tb_ti_layering](style_tb_ti_layering). `tools/ti/CLAUDE.md` and `tools/tl/CLAUDE.md` were already fully covered by [ti_module](ti_module) and [tl_module](tl_module) respectively; only a stale cross-reference in `ti_module` (pointing at `tools/tb/CLAUDE.md` by path) was repointed to the new page. All three `tools/*/CLAUDE.md` files removed — the wiki is now the single source for these conventions.

## [2026-07-23] revision | th HTTP API — dropped th.db dependency, polling goes straight to /tmp

Same-day revision of the note below. The user separately confirmed intent to remove `th.db` (SQLite) entirely, moving run history to [tl_module](tl_module) via fire-and-forget REST events. Rather than complicating the planned `GET /runs` routes, this simplified them: "in progress" status is inherently ephemeral local state that was already file-backed (`.status`/`.out`/`.log` in `/tmp`, written by `spawnDetached` independent of the DB), so `GET /runs` becomes a glob over `/tmp/th-*.status` instead of a `listRuns()`/`db.ts` extension. That plan is superseded. Historical runs beyond what's on disk stay explicitly out of scope for `th`'s own API — that gap is `tl`'s to fill once wired up. Updated in [th_cli](th_cli), [roadmap](roadmap), [style_dual_entrypoint](style_dual_entrypoint).

## [2026-07-23] ingest | th HTTP API — planned scope (POST /run detached + polling routes)

Design discussion, not yet implemented. Agreed with the user that `th`'s HTTP API is not a 1:1 mirror of the `tb`/`ti` pattern in [style_dual_entrypoint](style_dual_entrypoint): `th run` is long-running, so `POST /run` is always detached, and status/output are split across `GET /runs/:id` (row only), `/runs/:id/out`, `/runs/:id/log` so polling stays cheap. `GET /runs` gains a `status` filter alongside the existing `member` filter. Member/hats creation routes (`POST /member`, `GET /member`, `GET /member/:name`, `GET /hats`) included since the API must be usable without local CLI access to pre-existing members. `sandbox-exec`, `models`, `delete`/`promote`/`--from` explicitly excluded as local-only. Recorded in [th_cli](th_cli), tracked in [roadmap](roadmap) immediate todo.

## [2026-07-23] ingest | tb serve / ti serve — dual CLI+HTTP API entrypoint

## [2026-07-22] ingest | Skill rename decision: mythological figures to professions

Documented in [agenti](agenti) the decision to rename all 14 skills from mythological/historical-figure names to professions (mix of historical/real and fantastical). Full 1:1 mapping recorded in the "Available agents" table's new "Planned rename" column and expanded in the new section "Naming rename — mythological figures to professions". Two alternatives were considered and rejected first: plain functional names (technically sufficient for dispatch, which matches on description not name, but flattens per-role personality) and D&D classes (fully mapped, rejected as unconvincing). Decision record only — no `skills/`, `SKILL.md`, `th` member, `alfred.md`, or `CLAUDE.md` file has been touched yet; the actual rename proceeds incrementally, skill by skill, in follow-up sessions.

## [2026-07-21] implement | Third Identity (ti): live smoke test, bug fixes, packaging done
Live-tested `ti` end-to-end against the real Qdrant/Ollama instance on the Rasp (add → search → list → append-do → delete). Found and fixed three real bugs invisible to the mocked test suite (mocks matched the code's assumptions, not the API): `query`/`scroll` responses wrap points in `result.points`, not a bare `result` array; `query` needs `with_payload: true` explicit to return payload; single-point GET returns an object at `result`, not an array — `appendDo` read `result?.[0]` and would have thrown "No entry found" on **any** real existing id, a genuine blocking bug. Fixed in `qdrant.ts`/`identity.ts`, test mocks updated, full cycle re-verified live. Packaging closed out: `ti` registered in `package.json`'s `bin`, symlinked at `~/.local/bin/ti`, added to `setup.sh` alongside `tb`/`th` (idempotent on re-run). Flagged, not yet actioned: `ti` is a new procedural-memory system that neither the skills (`~/.claude/skills`, esp. Platone) nor `alfred.md` know about yet — both need updating to actually route procedural if/do decisions into `ti`. See [ti_module](ti_module).

## [2026-07-21] implement | Third Identity (ti): module implemented via tdd-coding flow
Built `tools/ti/` end-to-end via Annibale's tdd-coding flow: stub by efesto (signatures + per-function CONTRACT comments stating what isn't derivable from the signature) → 7 behavioural tests by knuth-black (mocked HTTP client, no live Qdrant/Ollama) → implementation by efesto (2 iterations — the first died silently mid-reasoning without writing code) → parallel review, knuth-black (black) + lusk-white (white): white confirmed full conformity to the founding spec, black found two real bugs — `appendDo` crashed with an unhandled `TypeError` on a non-existent id, and `addEntry`'s Qdrant request body didn't wrap the point in the `{ points: [...] }` shape the real upsert endpoint requires. Both fixed; the fix also generates the point id client-side (`crypto.randomUUID()`) since Qdrant's upsert response doesn't return it. Per user request, also split the original single-file `cli.ts` into a layered `cli.ts → identity.ts → qdrant.ts` architecture mirroring `tools/tb/CLAUDE.md`'s standard (`types.ts` unchanged) — a deviation from the founding README's single-file sketch, made after review flagged the flat version as a governance violation. Final state: `tools/ti/src/{types,qdrant,identity,cli}.ts`, `tests/ti.test.ts` (7/7 passing), `tsc --noEmit` clean. Not yet done: `pi_identity` collection provisioning, `package.json` bin registration. See [ti_module](ti_module).

## [2026-07-21] ingest | Third Identity (ti) and Third Log (tl): founded via Archimede (reconstructed entry)
**This entry reconstructs one accidentally destroyed**: a background agent wiped `.wiki/log.md` with a broken `sed`/`mv` while trying to append to it; the file was restored from git HEAD, which did not yet contain the original entry. This reconstruction is based on [ti_module](ti_module) and [tl_module](tl_module), not on the lost original text.

`ti` (Third Identity): a thin CLI over a dedicated Qdrant collection (`pi_identity`) storing context→action rules (`if`/`do`/`tags`) — distinct from `tb`'s semantic memory, which answers "what do I know about X" rather than "what should happen given context X". A `do` entry can be a behavior, a dispatch to a skill/`th` member, or a `tb` lookup hint (a search term, never a frozen note id). Deliberately a dumb client: no auto-merge, no similarity judgment, no internal LLM call — the merge/dedup decision stays with the caller, same discipline as `tb search` → `tb add`. Reuses `tools/tb/src/infra.ts` without modification.

`tl` (Third Log): a unified, structured event log extracted out of `th`'s local SQLite tracking, shared by `th`, `tb`, `ti`, and future producers. Not a CLI — a REST API (`Bun.serve`, hosted on the Rasp) called via `curl`/HTTP, SQLite-backed (`TL_DB`), a single `events` table with a fixed envelope (`id`/`timestamp`/`source`/`actor`/`context`/`outcome`/`tags`) plus a free-form `metadata` payload — deliberately one table, not one per source, since the point is that one query spans every source. Fire-and-forget client behavior: short timeout, zero retry, failures degrade to "no log entry" and never block the caller.

Both founded via Archimede in the same design conversation; both concretize gaps identified in [procedural_memory_gaps](procedural_memory_gaps).

## [2026-07-20] ingest | Orchestrator: systemd unit for the orchestrator process itself
Added `tools/orchestrator/deploy/orchestrator.service` — native systemd (`Restart=always`), not a container, to keep the orchestrator process itself alive on the Rasp. Deliberately different from conduwuit/Qdrant/Ollama (containerized): filesystem-as-truth state and nested `bwrap` sandboxing both want the host disk/kernel directly. Documented in [rasp_node](rasp_node), provisioning step 4.

## [2026-07-20] ingest | Orchestrator: silent-catch fix, year-rollover bug, NATS-vs-Matrix decision
Live-tested the orchestrator end-to-end (registered a task, forced `PASS`, watched the tick). Found and fixed a real bug: `executor.ts`'s instance-claim `catch {}` swallowed every transition error, not just the expected "already claimed" race — introduced `InstanceNotFoundError` in `queue.ts` so real faults now log instead of leaving an instance silently stuck in `pending` forever. Found and reproduced a second bug, deferred (recurring-only schedules never hit it): `dueSlot()`'s year-less cron matches "last year's" occurrence as due before today's slot arrives, causing a spurious extra run for any test with an absolute future date. Decided against introducing NATS for event-driven task activation — Matrix remains the control plane; revisit only if a real multi-consumer fan-out need appears. See [orchestrator_overview](orchestrator_overview) and [roadmap_orchestrator](roadmap_orchestrator).

## [2026-07-17] reconcile | Ping one-shot as reconciliation + new boot-timeout semantics

Adopted the one-shot ping as a level check in the scheduler tick, after an archaeological diagnosis (Indiana) showed three faces of the same defect: dispatch coupled only to the `i_wake` event (tasks due while the Desktop was already awake were never dispatched), the early-wake hole (`i_wake` ahead of schedule found `pending/` empty), and a tautological deadline (= the task's due time, already past when the instance materializes → alert ~30 s after the first WoL). Changes: new required dep `ExecutorDeps.pingHost` (prod: `ping -c 1 -W 2` in `main.ts`); `evaluateWakeWindow` pings when relevant desktop work exists — if up, direct dispatch and `wake.json` deleted, no WoL; if down, a WoL/retry/alert state machine with deadline `sentAt + ORCH_BOOT_TIMEOUT_MIN` (new env, default 5 min); `computeWakeAction` simplified to `(state, now, bootTimeoutMin)`; tick hardened (anti-overlap guard + caught rejections — an in-flight tick after `stop()` contaminated the next suite). The `i_wake` callback stays the wake event; the ping is reconciliation on top, not a wait loop — the distinction is now explicit in Pillar 3. 59 green tests (2 new integration tests on reconciliation). Updated: `orchestrator_overview` (Pillar 3: intro, new Ping Reconciliation bullet, Failure bound superseded; lifecycle Activation), `roadmap_orchestrator` (Boot-Timeout revised, new Ping Reconciliation entry, env).

## [2026-07-17] reconcile | Orchestrator Phase 2 (Nervous System) implemented and accepted after th cycle

Implemented the full Phase 2 in `tools/orchestrator/` via the tdd-coding flow (architecture lusk-white + turing-green → stub/implement efesto → 23 black-writer tests → parallel review knuth-black + lusk-white → fix round → probe re-verification: merge-ready, 58 green tests total). New modules: `wol.ts` (magic packet by hand on `node:dgram`, zero dependencies), `wake.ts` (wake window + `wake.json` single state file `{sentAt, attempts, alerted}`), `dispatch.ts` (scp/ssh plan as pure data), `executor.ts` (the only side-effectful module: local via `spawnSandboxed` in-process, remote scp→`th sandbox-exec`); `POST /i_wake` wired; systemd template in `deploy/`. Chosen architecture: "pure data pipeline" — event-driven dispatch on `i_wake`, state derived from the queue, side effects behind injectable deps (tests assert data, not mocks). Notable fixes from review: shell injection (`shell: true` removed), corrected WoL semantics (lead computed from the *catalog's* upcoming runs, not the queue; deadline = task expiry, not `sentAt+lead`), removed the 1 s time guard dictated by tests (temporal-coupling anti-pattern), `recover()` collision guard without renamed files, absolute paths and no CLI hop in the local path. Rejected SQLite (contradicts the filesystem-as-truth pillar); O(N)/tick-drift note added to Phase 4. Exposed (not caused) a pre-existing tsc error in `tools/th/src/db.ts`. Updated: `roadmap_orchestrator` (Phase 2 → DONE, two new Phase 4 entries), `orchestrator_overview` (Pillar 3: wake window from catalog, deadline and alert-once).

## [2026-07-16] ingest | Optional schedule controlled by Matrix (set_schedule/pause)

Design decision fixed after discussion: (1) `schedule` becomes optional at registration — a task without one is registered and audited but never scheduled, invocable only via `run_task` (primary use case: on-demand tasks); (2) after registration the schedule is owned by the catalog entry, the constant exported in the script is only the initial value — the code is audited and immutable, the policy (when it runs) is the user's and mutable, so rescheduling does not require re-audit; (3) new Matrix commands in Phase 3: `set_schedule <name> <cron>` and `pause <name>`, both Matrix-only, never REST (controlling the schedule = controlling execution, same reason as `run_task`); no notification mechanism needed, the scheduler re-reads the catalog every tick (10 s). Out of scope: stopping a running instance (executor territory, Phase 2/4). Impact on Phase 1 code when it lands: `metadata.ts` regex and `RaspberryTask` type with optional schedule, guard in `scheduler.ts`, `updateSchedule` in `catalog.ts` as a twin of `updateVerdict`. Updated: `roadmap_orchestrator` (Phase 3, Matrix Bridge — two new settled points), `orchestrator_overview` (Metadata section, Pillar 4 — schedule ownership note).

## [2026-07-16] reconcile | Orchestrator Phase 1 implemented and accepted after th review cycle

Implemented the whole Phase 1 (The Skeleton) in `tools/orchestrator/src/` — types, metadata (static regex parsing, never `import()`), catalog (`registered/` + `scripts/`, presence = registration), FS-queue (4 directories, `renameSync`, `recover()`), scheduler (interval loop, only `PASS` schedulable, dedup on all 4 states), REST API (`add_task`/`list_tasks`/`get_task_status`, `ORCH_PORT` default 7717, `ORCH_DIR` default `~/.pi/orchestrator`) — with 35 tests in `tests/orchestrator.test.ts`. Design facts fixed at implementation: `schedule` is a cron expression (package `croner`) validated at ingestion; catalog/queue reads resilient to corrupt JSON (skip + log); duplicate names rejected; entries start `UNAUDITED` (audit stays Phase 3, but the only-PASS-executes invariant is alive from day one). The module went through a full review cycle via `th`: build (efesto) → parallel adversarial review (knuth-black, probe-confirmed) + coherence audit (lusk-white) → synthesis (von-neumann-blue: accept-after-fixes) → fix round (B1-B4, C1-C4, T1-T7) → re-verification on the original probes → merge-ready. Three deferred findings tracked in roadmap (multi-instance TOCTOU and id collision in `recover()` → Phase 2; body limit → Phase 4) plus the NF-1 follow-up (per-task isolation in the scheduler's `tick()` → Phase 4). Updated: `roadmap_orchestrator` (Phase 1 → DONE with file references, deferred to Phase 2/4), `orchestrator_overview` (pillar 2: parse resilience; pillar 4: cron format), `roadmap` (worksite 1 → done).

## [2026-07-16] reconcile | th sandbox-exec implemented, sandbox fallback made noisy

Implemented the `th sandbox-exec -- <bin> <args...>` subcommand (`tools/th/src/cli.ts`, `passThroughOptions`): a thin wrapper over `sandboxExec` (`tools/th/src/runner.ts`) that runs an arbitrary binary in the bwrap sandbox forwarding stdio and exit code. Unlike `th run`, it refuses with an explicit error if bwrap is missing — an audited task must never run without a sandbox in silence. At the same time removed the pre-existing silent degradation: `ensureSandboxed` and `spawnSandboxed` now warn on stderr when they fall back to unsandboxed execution. Verified E2E: exit code propagated, writes outside the bind paths blocked (read-only filesystem), 41 green tests. Also fixed an orphan `}` in `roadmap_orchestrator` Phase 3. Updated: `roadmap_orchestrator` (Phase 2, task checked), `orchestrator_overview` (pillar 4, entrypoint from missing to built), `th_cli` (new sandbox-exec section, note on `th run` warning, cross-reference).

## [2026-07-16] ingest | Interactive pi chat via Matrix relay: /wake, /await, room-session

Added the "Interactive pi Chat (Matrix Relay)" section to `orchestrator_overview`: beyond the deterministic commands, the Matrix bot supports an interactive session with the `pi` agent on the Desktop — the last function an openclaw-style agent would have covered. Flow: `/wake` on Matrix → WoL → Desktop boots → `i_wake` callback → chat with `pi` through the room. Only new component: the relay (bot on the Rasp that forwards room messages to a `pi` session on the Desktop via SSH and relays replies back). Design constraints: (1) Matrix-only by construction — chatting with an agent is arbitrary execution by definition, same rationale as `run_task` (pillar 5); (2) one room = one persistent `pi` session with context continuity, not one-shot prompts; (3) idle-shutdown handled by the Desktop's ~30 min timer, with explicit override `/await <duration>` — chosen over an automatic heartbeat because explicit and deterministic, implementable as a timestamp file consulted by the idle-timer or `systemd-inhibit` with timeout. Updated: `orchestrator_overview` (new section), `roadmap_orchestrator` (Phase 3, pi Chat Relay task), `rasp_node` (conduwuit row), `roadmap` (Phase 8, openclaw replacement closed).

## [2026-07-16] reconcile | openclaw removed from Phase 8

Resolved the lint's TBD: `openclaw` is no longer needed. The orchestrator's scheduler plus Matrix as the control surface cover everything an agent of that kind would have done — scheduled tasks, ad-hoc triggers, notifications — deterministically and inside the Tailscale perimeter. Updated: `roadmap` (Phase 8).

## [2026-07-16] reconcile | Post-lint cleanup: rasp_node, Worksites & order, alignments

Applied the six findings of the health-check. (1) `agenti`: added `vasari` (CV Typst, operative skill without a hat), count 13→14. (2) `architettura`: documented `tb`'s real backing services (Qdrant for vectors+payload, Ollama for embedding, via `QDRANT_URL`/`OLLAMA_URL`) — before the page did not name them at all; corrected "immutable" to "additive" (`tb update --add-ref/--tags` exists). (3) Frontmatter of `roadmap` and `index`: removed the root `roadmap.md` source, no longer in the repo. (4) `openclaw` in Phase 8 marked TBD — named in the original plan but never defined anywhere, to clarify or remove. (5) New page `rasp_node`: single view of the services on the Rasp (orchestrator, conduwuit, Qdrant, Ollama, Tailscale) with network position and provisioning order — before they were spread across three pages. (6) New "Worksites & order" section in `roadmap`: order and dependencies between the three open worksites (orchestrator Phase 1 → TB-on-Rasp migration → procedural memory), with the Matrix layer as a shared component to build once. Updated index and cross-references.

## [2026-07-16] ingest | Third Brain on the Rasp, Matrix bot for agent-free queries — Phase 8 revised

Created new page `tb_on_rasp`: the TB is hosted on the Rasp and queried from anywhere via a dedicated Matrix bot, without an agent. Nearly free because `tb` is already fully client/server (notes as payloads in Qdrant via HTTP, embedding via Ollama via HTTP, no local state in the CLI — `tools/tb/src/infra.ts`): just repoint `QDRANT_URL` and `OLLAMA_URL` from desktop and laptop, inside Tailscale. Single source of truth, no sync problem. Reading needs no intelligence (`tb search` is deterministic retrieval → bot command `!tb search`); the agent stays needed only for writing/distillation (Platone), deferrable. Target configuration: two containers on the Rasp (Qdrant + Ollama), all clients point there; `nomic-embed-text` (~137M params) runs fine on Pi 4/5 via Ollama ARM64. Mass re-embedding (corpus migration/model change) runs from the Desktop without moving data — `OLLAMA_URL` local, `QDRANT_URL` toward the Rasp — and is exactly a `requiresDesktop: true` orchestrator task: WoL, job, shutdown. Revised roadmap Phase 8: interface from Telegram to a dedicated Matrix bot, because a Telegram bot would expose the TB outside the Tailscale perimeter to anyone holding the token (same rationale as pillar 5). Updated: `roadmap` (Phase 8, cross-reference), `orchestrator_overview` (cross-reference), `index` (new entry).

## [2026-07-16] ingest | Adopted approach for procedural memory: log first, intelligence after

Extended `procedural_memory_gaps` with the "Adopted approach" section: evidence-driven growth, three moves in cost order. (1) Unify the event log by extending `th.db` with a generic `events` table (member or skill, context, outcome, pointers to out/log) — solves gap 5 and reduces gap 1 to a Claude Code hook; (2) human-in-the-loop procedural extraction ("procedural Platone" that distills the action sequence from a successful run's log), with procedures saved in `tb` kind `protocollo` as a minimal store — dedicated store only with evidence that semantic retrieval is not enough; (3) revision and member↔skill promotion cycles deferred until months of accumulated events exist. Anti-pattern made explicit: starting from moves 2-3 without move 1 means building intelligence on data that does not exist. Next concrete step: schema of the `events` table + skill hook, to be founded as a module via Archimede. Same principle as the Raspberry Orchestrator: static analysis first, sandbox after — log first, intelligence after.

## [2026-07-16] ingest | Procedural memory gaps: th members and skills

Created new page `procedural_memory_gaps`: analysis of what is missing for `th` members and skills to learn from outcomes, distinct from the declarative memory already covered by `tb`. Clarified that Phase 2C (done, SQLite tracking of every `th run`) and Phase 7 (planned, `th stats` aggregated per hat) are only an evaluation/scoring layer — they do not extract, store, or revise reusable procedures, and they do not touch the skill system at all.

Identified six gaps in dependency order: (1) no telemetry on skills — only `th` members are tracked; (2) no extraction of the procedure from the outcome — a score is not the action sequence that worked; (3) no trigger-indexed store — `tb` indexes by semantic similarity, skills by hand-written description, an index derived from data is missing; (4) no revision/versioning loop — `tb` is only additive, skills update only by hand; (5) no bridge between `th.db`, `skills/` and `tb` — three systems without shared schema or identifiers; (6) no member↔skill promotion pipeline, analogous to `th member promote`.

Dependency note: gap 1 is the prerequisite for gaps 2-6 (skill data is missing); gap 5 is the likely architectural bottleneck — as long as the three stores stay isolated, any fix to gaps 2/4/6 risks being built three times.

Updated: `roadmap` (scope note on Phase 7, cross-reference), `agenti` (cross-reference), `index` (new entry).

## [2026-07-16] ingest | Why the bot must be Matrix, not Telegram

Added to pillar 5 (Access Control) the motivation, until now implicit, for why the control-surface bot must stay Matrix self-hosted and not a Telegram bot: the constraint is not "Matrix" itself, but that the bot lives in the same Tailscale-only perimeter as everything else. The Matrix homeserver (conduwuit, Rust) runs on the Rasp itself and is reachable only via Tailscale — same perimeter, different protocol on top, not a parallel channel. A Telegram bot would violate this: Telegram's Bot API is hosted on Telegram's servers, reachable from any network by anyone holding the token — it never crosses the tailnet, so it would open a second parallel ingress that bypasses the pillar 5 ACL, voiding the threat-model reasoning. Secondary benefit: Tailscale logs every connection crossing the tailnet, so the self-hosted homeserver stays inside that visibility; a Telegram bot's traffic would never appear there. Updated: `orchestrator_overview` (pillar 5).

## [2026-07-15] reconcile | run_task removed from REST, exclusive to the Matrix bot

Removed `run_task` from the Rasp's REST surface. Motivation: the Tailscale ACL perimeter protects the network but does not distinguish roles between authorized devices (Desktop, laptop, phone) — all see the same REST surface (pillar 5). An HTTP endpoint for ad-hoc execution means a single compromised device on the tailnet could launch any registered task at any time. With the endpoint removed, the manual trigger passes only through a Matrix bot command, gated by the homeserver's authentication — a channel independent of tailnet membership, not just a confirmation layer on top of REST. The programmatic trigger case (from a script) is also covered without reintroducing the endpoint: just send a Matrix message instead of an HTTP call.

`i_wake` stays on REST, deliberately: it dispatches only tasks already due by schedule, not arbitrary ones — the maximum damage of an abuse is advancing an already-scheduled execution, not executing anything. Different risk category from `run_task`, does not need the same treatment.

Updated: `orchestrator_overview` (API Surface, pillar 5, Task Lifecycle Flow point 4, Control Surface), `roadmap_orchestrator` (Phase 1 — Basic REST API, Phase 3 — Matrix Bridge).

## [2026-07-15] reconcile | Ingestion via REST API, not scp/SSH — access control model rewritten

Replaced entirely the ingestion model described in previous pages (direct scp into `/scripts` + per-device SSH key, never confirmed by the user and inconsistent with the real design) with the model confirmed in conversation: the Rasp exposes a minimal REST service as the only entry point, without scp and without direct filesystem writes by devices. Endpoints: `add_task` (submits a script to audit, registers in `available_tasks` if PASS/WARNING-confirmed), `i_wake` (called by the Desktop at boot, replaces the old generic POST), `list_tasks` and `get_task_status <id>` (read-only), `run_task <name>` (ad-hoc start, only on already-registered tasks — no path to execute unaudited code).

Introduced explicitly the distinction between two filesystem state levels (no DB, for small single-user scale and inspectability with cat/ls): the `available_tasks` catalog (one JSON file per registered script, whose presence is the registration) and the execution queue `pending/processing/completed/failed` (already present, unchanged — position-as-state, `fs.renameSync`).

Reformulated Pillar 5 (Access Control): no longer Tailscale ACL + per-device SSH key with differentiated write permissions on `/scripts`, but a single network perimeter (Tailscale) that decides who can reach the REST API — no filesystem permission model, because there is no longer a filesystem write path exposed to devices.

Made the Matrix bot bidirectional: beyond notifications, commands (`list_tasks`, `run_task`, manual Desktop wake) that operate on the same structures as the REST API, not a parallel system. The manual wake stays an explicit trigger, separate from the scheduler's automatic WoL logic.

Fixed a broken cross-reference: `roadmap_orchestrator` pointed to `[raspberry-orchestrator]`, a page/file no longer in the repo (the original source `raspberry-orchestrator.md` was deleted in a commit before this revision) — replaced with `[orchestrator_overview]`. Frontmatter of both pages updated from `sources: [raspberry-orchestrator.md]` to `sources: [conversation]`, to not attribute to a source document content that actually comes from direct conversation with the user.

Updated: `orchestrator_overview` (Core Concept, new API Surface section, Pillars 2, 3, 5, Task Lifecycle Flow, Technical Stack), `roadmap_orchestrator` (Phases 1, 2, 3, cross-reference).

## [2026-07-15] reconcile | Script metadata as exported constants, not JSDoc tags — static parsing required

Replaced the format of the scripts' operational metadata (Pillar 4): no longer JSDoc tags (`@requiresDesktop`, `@schedule`) but constants exported at the top of the file (`export const requiresDesktop = true`, `export const schedule = "..."`). Critical point: these constants must be read with static parsing (regex or TS compiler API/AST), never with dynamic `import()` — an import runs the module's top-level code as a side effect, which would run not-yet-audited code before Pillar 1 (Adversarial Audit) has given a verdict. Same principle already applied to the audit itself ("reads the code, does not run it"), now made explicit as a systemic principle, not just of the audit call. Also confirmed two minor decisions with no design impact: Desktop MAC parametric via env var (value to fix at implementation); Matrix approval channel entirely on Tailscale, homeserver on the Rasp, single user — no extension to Pillar 5. Also confirmed: scripts with a `FAIL` verdict are deleted, no retention for audit trail. Updated: `orchestrator_overview` (pillars 1 and 4, Task Lifecycle Flow), `roadmap_orchestrator` (Phase 1 — Task Type Definition, Phase 2 — WoL Module, Phase 3 — Adversarial Audit Integration and Matrix Bridge).

## [2026-07-15] reconcile | WoL network topology confirmed and access control model for task ingestion

User confirmed the physical topology: Rasp and Desktop connected via Ethernet on the same LAN, ~10cm apart — the WoL magic packet travels on local L2 broadcast, no Tailscale/WireGuard traversal. Tailscale stays reserved for external access to the orchestrator (laptop, phone), not for Rasp↔Desktop communication. Added a fifth architectural pillar, Access Control (The Perimeter): no custom user/role system in the app, two levels both already existing — Tailscale ACL per device tag (limit which devices reach the Rasp's SSH/HTTP ports) and a dedicated SSH key per device in `authorized_keys` (Desktop and laptop with full write on `/scripts`, phone with a read-only forced command, excluded from registering new tasks). Motivation: the endpoint is never exposed publicly, only on Tailscale — an attacker would first have to breach the Tailscale network itself, so the two levels are sufficient defense in depth. Updated: `orchestrator_overview` (new pillar 5, topology note in pillar 3, technical stack), `roadmap_orchestrator` (Phase 2 — topology note resolved, Phase 3 — Access Control Setup task), `index` (pillar count).

## [2026-07-15] reconcile | th sandbox-exec: missing CLI entrypoint for remote sandboxing

Clarified pillar 4: `spawnSandboxed` (in `tools/th/src/runner.ts`) is already exported but today used only internally by `th run` to launch the `pi` agent — `th`'s CLI (`tools/th/src/cli.ts`: `member`, `hats`, `run`, `wait`, `models`, `history`, `get`) exposes no subcommand to wrap an arbitrary binary. Need to build `th sandbox-exec -- <bin> <args...>`, a thin wrapper that calls `spawnSandboxed` and forwards stdio/exit code — no new file to ship to the Desktop, because Rasp and Desktop already mount the same `pi`/`th` stack, only the entrypoint is missing. Also clarified the full remote sequence: `scp` of the script (already planned, because the script is born and audited on the Rasp and does not exist locally on the Desktop) followed by `ssh ... th sandbox-exec bun run <path>` instead of a bare `bun run` — two orthogonal steps, one moves the file, the other decides how it is launched. Locally on the Rasp no CLI hop: `spawnSandboxed` called directly in-process. Added the subcommand as an explicit task in Phase 2 of the roadmap, prerequisite of the SSH execution wrapper. Updated: `orchestrator_overview` (pillar 4), `roadmap_orchestrator` (Phase 2).

## [2026-07-15] reconcile | Reuse of th's bwrap sandbox for task execution

Pillar 4 (Deterministic TS Execution): task execution, both local (Rasp, `Bun.spawn`) and remote (Desktop, `ssh` + `bun run`), is wrapped in `th`'s existing bwrap sandbox (`spawnSandboxed`, `tools/th/src/runner.ts`), reusing the real bind profile as-is (`cwd`, `~/.pi`, `~/.bun`, `/tmp`) — no path escape, no network isolation, because these tasks have already passed the audit (`PASS`) and must write real data. Sandbox distinct from the audit's Docker one (pillar 1): Docker tests adversarially before the queue, bwrap executes trustedly after. Works without extra wrappers because Rasp and Desktop mount the same `pi`/`th` stack. Updated: `orchestrator_overview` (pillar 4), `roadmap_orchestrator` (Phase 2, Local Execution Path and Provisioning Pipeline).

## [2026-07-15] reconcile | Human-readable summary for every loaded task

Added to pillar 1 (Adversarial Audit): the same call that produces the PASS/FAIL/WARNING verdict also generates a short summary (description + numbered steps) of what the task does, read from the code statically — no second LLM call. The summary is sent to Matrix for every task loaded, regardless of verdict: informational only, does not block execution (only `WARNING` still requires human approval). Updated: `orchestrator_overview` (pillar 1), `roadmap_orchestrator` (Phase 3).

## [2026-07-15] reconcile | Wake-window scheduling and gradual audit for the Raspberry Orchestrator

Simplified the Boot-Callback Pattern: no more indefinite wait (`AWAITING_BOOT`). The Rasp computes the wake time from the nearest scheduled task, sends a single WoL in advance (~30 min), the Desktop calls home at boot, the Rasp dispatches in batch all tasks within the wake window. If the callback does not arrive by the task's deadline: a single WoL retry, then a Matrix alert — single-threshold control, not a polling loop. Shutdown is decided locally by a systemd idle-timer service on the Desktop, not commanded remotely. The boot-timeout was moved from Phase 4 (hardening) to Phase 2 (core loop) of the roadmap — it is not an edge case, it is the first scenario that happens in production.

Clarified the Adversarial Audit (pillar 1): the current implementation is static analysis (the cloud `pi` agent reads the code and reasons adversarially, no execution). Documented as an optional future evolution the dynamic execution in an ephemeral Docker sandbox (no egress network, decoy filesystem mimicking production paths) — to implement after the static version, not in its place.

Updated: `orchestrator_overview` (pillars 1 and 3), `roadmap_orchestrator` (Phases 2, 3, 4).

## [2026-07-14] ingest | orchestrator_overview

## [2026-07-14] ingest | roadmap_orchestrator

## [2026-07-02] reconcile | Ghost bin cleanup and inventory alignment

Removed the ghost tools `td` (Third Done) and `mvr` (multiversal rules game): sources absent from the repo, `bin` entry of package.json and symlinks `~/.local/bin/{td,mvr}` deleted, `setup.sh` cleaned. `td` archived — only the legacy DB `~/.pi/td.db` survives; the `taiichi` skill no longer exists. Updated: `architettura` (table of 3 live layers + archive note, storage), `agenti` (added `archimede` and `postino` → 13, aligned to `skills/`), `roadmap` (Phase 2 → Done, Phase 4 → Archived, per-hat metrics moved to Phase 7, phase detail numbering realigned to the table). Historical entries on `tw` stay unchanged. Sources in the repo aligned in parallel (README, roadmap, setup.sh, package.json).

## [2026-06-14] reconcile | Collapse of the tw tool into the Omero skill

The `tw` tool (tools/tw/) was deleted: the wiki is now managed entirely by the Omero skill, which operates directly on `.wiki/` with Read/Write/Edit/Glob/Grep — no CLI, no global registry. Deleted the `tw_cli` page (documented deleted code). Updated `architettura` (layer table, storage, setup, cross-ref), `index`, `roadmap` (Phase 5, note ‡, immediate todo).

## [2026-06-06] init | First wiki creation

Created the wiki from scratch: pages architettura, agenti, th_cli, tw_cli, roadmap, log.
Ingested material: README.md, roadmap.md, alfred.md, tools/th/src/cli.ts, tools/th/src/runner.ts, tools/tw/src/cli.ts, tools/tw/src/wiki.ts.
